diff --git a/node_modules/expo-gl-cpp/android/build.gradle b/node_modules/expo-gl-cpp/android/build.gradle
index 025d491..e508864 100644
--- a/node_modules/expo-gl-cpp/android/build.gradle
+++ b/node_modules/expo-gl-cpp/android/build.gradle
@@ -50,7 +50,7 @@ buildscript {
 
 // Creating sources with comments
 task androidSourcesJar(type: Jar) {
-  classifier = 'sources'
+  archiveClassifier = 'sources'
   from android.sourceSets.main.java.srcDirs
 }
 
@@ -72,7 +72,7 @@ afterEvaluate {
 }
 
 android {
-  compileSdkVersion safeExtGet("compileSdkVersion", 31)
+  compileSdk safeExtGet("compileSdkVersion", 31)
 
   compileOptions {
     sourceCompatibility JavaVersion.VERSION_11
@@ -92,7 +92,7 @@ android {
     externalNativeBuild {
       cmake {
         abiFilters (*reactNativeArchitectures())
-        arguments "-DREACT_NATIVE_DIR=${REACT_NATIVE_DIR}", "-DRN_SO_DIR=${RN_SO_DIR}"
+        arguments "-DREACT_NATIVE_DIR=${REACT_NATIVE_DIR}", "-DRN_SO_DIR=${RN_SO_DIR}", "-DANDROID_STL=c++_shared"
       }
     }
   }
@@ -104,7 +104,7 @@ android {
   }
 
   buildFeatures {
-    // prefab true
+    prefab true
   }
 
   packagingOptions {
@@ -134,6 +134,7 @@ dependencies {
   compileOnly 'com.facebook.soloader:soloader:0.8.2'
 
   implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk7:${getKotlinVersion()}"
+  implementation 'com.facebook.react:react-android'
 }
 
 def extractReactNativeAAR = { buildType ->
@@ -150,14 +151,16 @@ def extractReactNativeAAR = { buildType ->
       throw new GradleException("Multiple React Native AARs found:\n${rnAARs.join("\n")}" +
           "\nRemove the old ones and try again")
     }
-  }
-  def rnAAR = rnAARs.singleFile
-  def file = rnAAR.absoluteFile
-  def packageName = file.name.tokenize('-')[0]
-  copy {
-    from zipTree(file)
-    into "$buildDir/$file.name"
-    include "jni/**/*"
+    def rnAAR = rnAARs.singleFile
+    def file = rnAAR.absoluteFile
+    def packageName = file.name.tokenize('-')[0]
+    copy {
+      from zipTree(file)
+      into "$buildDir/$file.name"
+      include "jni/**/*"
+    }
+  } else {
+    logger.warn("No React Native AAR found in ${RN_AAR_DIR}. Skipping extraction.")
   }
 }
 
diff --git a/node_modules/expo-gl-cpp/cpp/CMakeLists.txt b/node_modules/expo-gl-cpp/cpp/CMakeLists.txt
index 2c69484..f4ae208 100644
--- a/node_modules/expo-gl-cpp/cpp/CMakeLists.txt
+++ b/node_modules/expo-gl-cpp/cpp/CMakeLists.txt
@@ -34,13 +34,20 @@ if(NOT LIBRN_DIR)
     file(GLOB LIBRN_DIR "${RN_SO_DIR}")
 endif()
 
-target_include_directories(${PACKAGE_NAME}
-                           PRIVATE "${REACT_NATIVE_DIR}/ReactCommon/jsi")
+# Prefab support for RN 0.71+
+find_package(ReactAndroid CONFIG)
 
-find_library(
-    JSI_LIB jsi
-    PATHS ${LIBRN_DIR}
-    NO_CMAKE_FIND_ROOT_PATH)
+if(ReactAndroid_FOUND)
+    set(JSI_LIB ReactAndroid::jsi)
+else()
+    target_include_directories(${PACKAGE_NAME}
+                               PRIVATE "${REACT_NATIVE_DIR}/ReactCommon/jsi")
+
+    find_library(
+        JSI_LIB jsi
+        PATHS ${LIBRN_DIR}
+        NO_CMAKE_FIND_ROOT_PATH)
+endif()
 
 find_library(LOG_LIB log)
 find_library(GLES_LIB GLESv3)
diff --git a/node_modules/expo-gl-cpp/cpp/EXGLImageUtils.cpp b/node_modules/expo-gl-cpp/cpp/EXGLImageUtils.cpp
index 82cd012..b78008d 100644
--- a/node_modules/expo-gl-cpp/cpp/EXGLImageUtils.cpp
+++ b/node_modules/expo-gl-cpp/cpp/EXGLImageUtils.cpp
@@ -1,3 +1,4 @@
+#include <vector>
 #include "EXGLImageUtils.h"
 
 #define STB_IMAGE_IMPLEMENTATION
@@ -126,11 +127,11 @@ std::shared_ptr<uint8_t> loadImage(
     if (strncmp(localUri.c_str(), "file://", 7) != 0) {
       return std::shared_ptr<uint8_t>(nullptr);
     }
-    char localPath[localUri.size()];
-    decodeURI(localPath, localUri.c_str() + 7);
+    std::vector<char> localPath(localUri.size() + 1);
+    decodeURI(localPath.data(), localUri.c_str() + 7);
 
     return std::shared_ptr<uint8_t>(
-        stbi_load(localPath, fileWidth, fileHeight, fileComp, STBI_rgb_alpha),
+        stbi_load(localPath.data(), fileWidth, fileHeight, fileComp, STBI_rgb_alpha),
         [](void *data) { stbi_image_free(data); });
   }
   return std::shared_ptr<uint8_t>(nullptr);
